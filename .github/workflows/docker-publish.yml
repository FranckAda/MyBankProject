name: Build & Push Docker Images
 
on:
  push:
    branches: [main]
 
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
 
    steps:
      - name: Checkout
        uses: actions/checkout@v4
 
      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
 
      - name: Préparer le tag court
        id: meta
        run: echo "sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
 
      - name: Build & Push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/franckada/mybankproject-frontend:latest
            ghcr.io/franckada/mybankproject-frontend:${{ steps.meta.outputs.sha }}
 
      - name: Build & Push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/franckada/mybankproject-backend:latest
<<<<<<< HEAD
            ghcr.io/franckada/mybankproject-backend:${{ steps.meta.outputs.sha }}
 
=======
            ghcr.io/franckada/mybankproject-backend:${{ steps.meta.outputs.sha }}
>>>>>>> b61450ebb4801465e626f0232ede8dc4686cff94
